# 🖼️ Автоматическая оптимизация изображений

## Что было сделано

Добавлена автоматическая оптимизация всех загружаемых изображений блюд с использованием библиотеки **sharp**.

## Параметры оптимизации

### Размеры
- **Максимальная ширина**: 1200px
- **Максимальная высота**: 1200px
- Изображения масштабируются пропорционально без увеличения оригинала

### Качество
- **JPEG/JPG**: 85% (с использованием mozjpeg для лучшего сжатия)
- **PNG**: 85% с максимальным уровнем сжатия (9)
- **WebP**: 85%
- **GIF**: без изменений (для поддержки анимации)

### Лимит размера файла
- **До оптимизации**: 5 МБ (ограничение при загрузке)
- **После оптимизации**: обычно 100-500 КБ

## Как это работает

### 1. Загрузка изображения
```
Администратор загружает изображение → Multer сохраняет файл → Sharp оптимизирует → Сохраняется в БД
```

### 2. Умная оптимизация
- Оптимизированная версия создаётся только если она **меньше** оригинала
- Если оригинал уже оптимален, он сохраняется без изменений
- Временные файлы автоматически удаляются

### 3. Логирование
В консоли сервера выводится информация:
```
✅ Изображение оптимизировано: 3245.2 KB → 487.3 KB (экономия 85.0%)
```
или
```
ℹ️  Оригинал уже оптимален, изменения не применены
```

## Где применяется

Оптимизация применяется автоматически при:

1. **Создании нового блюда** (`POST /foods`)
2. **Обновлении изображения блюда** (`PATCH /foods/:id/image`)

## Преимущества

### 1. Скорость загрузки
- Уменьшение размера файла в 5-10 раз
- Быстрая загрузка страниц для клиентов
- Экономия мобильного трафика

### 2. Экономия ресурсов
- Меньше места на сервере
- Меньше расходов на хостинг
- Меньше расходов на CDN/трафик

### 3. Производительность
- Меньшая нагрузка на сервер при раздаче файлов
- Лучшая производительность для пользователей на медленных соединениях

### 4. Прозрачность
- Работает автоматически
- Не требует действий от администратора
- Сохраняет качество изображения

## Пример экономии

### До оптимизации
```
50 блюд × 3 МБ = 150 МБ
1000 просмотров = 150 ГБ трафика
```

### После оптимизации
```
50 блюд × 400 КБ = 20 МБ
1000 просмотров = 20 ГБ трафика
```

**Экономия**: 130 ГБ трафика (87%) ✨

## Технические детали

### Зависимости
```json
{
  "sharp": "^0.33.x"
}
```

### Файлы
- `/server/src/utils/imageOptimizer.ts` - утилита оптимизации
- `/server/src/controllers/food.ts` - интеграция в контроллер

### Настройка параметров
Параметры оптимизации можно изменить в `imageOptimizer.ts`:
```typescript
const DEFAULT_OPTIONS: OptimizeOptions = {
  maxWidth: 1200,    // максимальная ширина
  maxHeight: 1200,   // максимальная высота
  quality: 85,       // качество (0-100)
};
```

## Тестирование

Для проверки работы:
1. Запустите сервер: `npm run dev`
2. Загрузите изображение блюда через админ-панель
3. Проверьте логи в консоли сервера
4. Проверьте размер файла в `/server/uploads/images/`

## Рекомендации

### Для лучших результатов:
- ✅ Загружайте изображения в формате **JPEG** или **WebP**
- ✅ Используйте изображения хорошего качества (не нужно предварительно сжимать)
- ✅ Рекомендуемые исходные размеры: 1500-3000px по большей стороне

### Не рекомендуется:
- ❌ Загружать уже сильно сжатые изображения (могут получиться артефакты)
- ❌ Загружать GIF если нужна анимация (они не оптимизируются)
- ❌ Загружать скриншоты с текстом (лучше использовать PNG)
